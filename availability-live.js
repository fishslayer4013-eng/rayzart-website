// Emergency compatibility stub.
// Live RBMS availability sync is intentionally disabled on the production website.
// This file remains only so older cached pages that still request it receive harmless code.
(() => {
  window.RAYZART_LIVE_SYNC_DISABLED = true;
})();
